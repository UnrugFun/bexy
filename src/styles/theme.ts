const theme = {
  colors: {
    background: '#0A0A0A', // Slightly darker than suggested for better contrast
    text: '#F8F8F8', // Slightly off-white for better readability
    accent: '#FFA000', // A rich golden orange
  },
};

export default theme;